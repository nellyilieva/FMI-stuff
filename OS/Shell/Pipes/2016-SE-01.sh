#!/bin/bash

# Даден е текстов файл с име philip-j-fry.txt.
# Напишете shell script и/или серия от команди, които извеждат броя редове,
# съдържащи поне една четна цифра и несъдържащи малка латинска
# буква от a до w.

cat philip-j-fry.txt | grep -E '^[^a-w]*[2468][^a-w]*$' | wc -l